DATABASE_URL=sqlite:///./dados.db
SECRET_KEY=mude-esta-chave-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Banco PostgreSQL (usado pelo docker-compose). Preencha antes de subir o container.
POSTGRES_USER=admin
POSTGRES_PASSWORD=mude-esta-senha
POSTGRES_DB=visia_db
